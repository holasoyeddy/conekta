package conekta

import (
	"errors"

	"github.com/holasoyeddy/conekta/resources"
)

// CreateCharges ...
func (client *Client) CreateCharges(charge resources.Charge) error {
	return errors.New("conekta: not yet implemented")
}
